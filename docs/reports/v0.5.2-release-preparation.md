# v0.5.2 release preparation

## 1. Scope

Convert the exact pre-release-ready v0.5.2 state into the package and documentation state intended for the eventual published release. This report records release preparation only. No PR, merge, tag, GitHub Release, or npm publication was performed.

## 2. Repository

`C:\Users\daile\Projects\my-dev-kit-lab` (`dailephd/my-dev-kit-lab`; default branch `main`).

## 3. Source readiness evidence

- Starting branch: `feature/v0.5.2-warm-index-real-agent-campaigns`
- Starting readiness-state HEAD and origin branch: `099270d36f3c9fbed3546738045fc3a7c73a252e` (clean worktree)
- Source readiness record: `25093ae5997eaf486e5bc7b61f696fa34aa5406e`
- Validated candidate: `d40496e64d5a50be8542ce55d6e1bd91265e51c9`
- Prior verdict: `PASS_READY_FOR_RELEASE_PREP`
- Ordinary CI: `35923645081`, six jobs passed
- Dedicated latest-Node readiness: `35927114131`, Ubuntu/macOS/Windows passed
- Prior release blockers: 0

## 4. Release branch and preflight

Created `release/v0.5.2` from the exact starting SHA above. GitHub authentication was active for `dailephd`; repository identity and default branch matched. Registry baseline was `0.5.1`; `@dailephd/my-dev-kit-lab@0.5.2` returned npm 404/not found. Local and remote `v0.5.2` tags, GitHub Release `v0.5.2`, and local/remote release branches were absent before branch creation.

## 5. Version and lockfile

`npm version 0.5.2 --no-git-tag-version` changed only the intended version fields and did not create a tag.

- `package.json`: `0.5.2`
- `package-lock.json` root: `0.5.2`
- `package-lock.json` `packages[""]`: `0.5.2`
- Dependencies, devDependencies, engines, bin, and Playwright `1.60.0`: unchanged

## 6. Final documentation state

- `README.md` identifies v0.5.2 as latest/current, v0.5.1 as previous, package/CLI 0.5.2, and v0.6.0 as next planned. Real-agent campaigns are shipped/current; prior corpus behavior, legacy fake-agent behavior, v0.6.0 scope, and capability limitations remain documented.
- `CHANGELOG.md` retains an empty `## [Unreleased]` heading and has the complete v0.5.2 content under `## [0.5.2] - 2026-09-23`.
- `docs/CURRENT_STATE.md` records package/current release 0.5.2, previous 0.5.1, no implemented/unreleased version, v0.6.0 next, `main` as current branch, historical readiness evidence, and the next action to plan v0.6.0 index freshness/changed-file detection.
- `docs/ROADMAP.md` marks v0.5.2 published and summarizes shipped features without batch narration; the v0.6.0 section remains planned/not implemented.
- `docs/PROJECT_OVERVIEW.md`, `docs/ARCHITECTURE.md`, `docs/COMMANDS.md`, `docs/WORKFLOWS.md`, `docs/METRICS.md`, `docs/GALLERY.md`, and `docs/TUTORIAL.md` now describe the v0.5.2 campaign capability as released/current while retaining existing ownership, syntax, formulas, and limits.

## 7. Preservation manifest

- `currentFacts.latestPublishedVersion`: `0.5.2`
- `currentFacts.currentImplementedUnreleasedVersion`: `null`
- `currentFacts.nextPlannedVersion`: `0.6.0`
- `changelog.requiredPublishedReleases`: 25 entries, including `0.5.2`
- Required roadmap version count remains 39; no prior release, required topic, or document was removed.

## 8. Other documentation and lifecycle audit

Reviewed canonical docs and tracked user-facing Markdown, including the security validation framework, tutorial, preservation policy, and benchmark plan. The benchmark plan and preservation policy were left unchanged. Current-state transitional wording searches found no stale v0.5.2 lifecycle claims. Historical pre-release statements remain only in reports describing those earlier stages. No current document identifies v0.5.1 as latest/current.

## 9. Package hygiene

Inspected `package.json`, `.gitignore`, and `.npmignore`. `package.json.files` and ignore rules are unchanged. Generated `reports/`, `lab-output/`, `node_modules/`, `.my-dev-kit-context/`, browser caches, provider smoke outputs, `*.tgz`, `.env*`, local logs, IDE files, and local agent instruction files remain excluded/ignored according to repository policy. No generated audit/security report or tarball is tracked.

## 10. Local validation

| Command | Result |
| --- | --- |
| `npm ci` | PASS; no dependency drift, 0 vulnerabilities from install audit |
| `npm run docs:check` | PASS; 13 documents, 39 roadmap versions, 25 published releases |
| `npm run typecheck` | PASS |
| `npm run build` | PASS |
| Focused v0.5.2 regression command (12 named test files) | PASS; 12 files, 256 tests |
| `npm run test:tutorial-browser` | PASS; real Chromium, 1 test passed, not skipped |
| `npm test` | PASS; 389 files, 5,011 passed, 1 skipped, 0 failed |
| `npm run verify` | PASS; build and 12 benchmark checks |
| `node dist/scripts/cli.js --version` | PASS; exactly `0.5.2` |
| CLI help/describe smokes (`--help`, experiment run `--help`, warm-index describe `--json`, tutorial `--help`) | PASS; all exit 0 |

## 11. Exact packed-package gate

`npm run verify:packed-package` passed for `@dailephd/my-dev-kit-lab@0.5.2`; tarball SHA-256 `17082822ac6e629608fe6a29b1f1b06c8b47b5f5410d74ad23850a30d3a95426`. Warm-index legacy execution, reports, four plots, 12-case corpus, all Codex/Claude outcomes including token-unavailable/timeout, timeout-isolation continuation, provider argv privacy, report/plots/screenshot/gallery presentation, tutorial actions, target immutability, installed-package immutability, and bounded artifacts passed.

## 12. Package dry-run inventory

`npm pack --dry-run --json` reported package `@dailephd/my-dev-kit-lab@0.5.2`, 609 entries, 661,195 packed bytes, and 2,980,532 unpacked bytes. README, CHANGELOG, package metadata, built CLI, `dist/src/`, benchmarks, `docs/METRICS.md`, token cases, and tutorial example files are present. Tests, reports, `node_modules`, generated lab output, browser caches/binaries, provider fixture logs, environment files, IDE files, and tarballs were not present. Packed README names v0.5.2 current; packed CHANGELOG has the dated release section; packed METRICS has accurate shipped campaign semantics. No packed current-state content identifies v0.5.1 as latest.

## 13. Security and audit sanity

- `npm audit`: 0 vulnerabilities
- `npm audit --omit=dev`: 0 vulnerabilities
- `npm run test:security`: PASS; 44 files, 519 tests
- `npm run security:validate -- --format text,json`: `READY EXCEPT OPTIONAL MANUAL CHECKS`; 10 checks, 7 passed, 1 warned, 0 failed, 2 unavailable optional tools (OSV-Scanner and CodeQL), 5 informational findings, 0 release blockers
- `npm run security:package`: PASS; 1 passed, 0 warned, 0 failed, 0 findings
- `npm run audit -- --types code-rot --format text,json --fail-on none`: exit 0; 1,341 files scanned, 0 skipped detectors, 124 findings (0 blocker, 3 high, 9 medium, 57 low, 55 info), `releaseBlockingCount: 0`

The 3 high and 9 medium audit findings match the prior readiness findings: Android documentation source-of-truth false positives, frozen historical fixture references, and negative-test data. No new high/medium release-blocking finding was introduced by final release documentation.

## 14. Release-branch readiness

The existing `Pre-release latest Node readiness` workflow run `35982225571` ([run details](https://github.com/dailephd/my-dev-kit-lab/actions/runs/35982225571)) passed on exact SHA `70b829940dd1d57513c444024a6ec388b1d172c7`. Ubuntu completed in 7m13s, macOS in 7m32s, and Windows in 11m38s. Every required step passed on all three systems, including `npm ci`, Chromium installation, real-browser test, docs check, typecheck, build, audit tests, full test suite, verify, code-rot audit, security validation, and packed-package verification. The report update is documentation-only and will receive the required exact-SHA readiness run after push. No workflow was added or changed.

## 15. Initial packed-gate retry note

The first local packed-package run exhausted its internal retries after Windows Chromium `Page.captureScreenshot` errors. A clean rerun passed the complete gate, including screenshot and presentation acceptance. Chromium behavior and product code were not changed.

## 16. Release blockers

0. Local validation is complete. The exact-SHA workflow for the report-containing final commit and the final remote SHA/worktree checks remain required before the stage verdict.

## 17. Publication actions not performed

No release PR, merge to `main`, `v0.5.2` tag, GitHub Release, or `npm publish` was performed.

## 18. Final verdict

The initial release-preparation commit passed exact-SHA release-branch readiness on Ubuntu, macOS, and Windows. The documentation-only report correction remains subject to exact-SHA readiness before the final stage verdict.
