# v0.4.7 Installed-Package Correction

## Repository

- Repository: `C:\Users\daile\Projects\my-dev-kit-lab`
- Branch: `feature/v0.4.7-browser-process-foundation`
- Starting HEAD: `401c5a80f8676770c0a138c4b072c4e730db48b1`
- Ending HEAD: correction commit containing this report
- Correction commit SHA: local `HEAD` after commit
- Previous audit verdict: `NEEDS_CORRECTION`

## Gaps addressed

1. Playwright is now an exact runtime dependency rather than a development-only dependency.
2. The generic deterministic tutorial resources are canonical under `examples/tutorial-browser/`; the duplicated test-owned static resources were removed and the test helper consumes the canonical example.
3. `scripts/verify-packed-package.mjs` now exercises installed tutorial help, validation, isolated browser-unavailable behavior, real Chromium execution, artifact/manifest/path boundaries, workspace routing, and package/example immutability from the exact packed tarball.

## Package and example correction

- Package version remains `0.4.6`.
- Playwright before: devDependency-only, `^1.54.1`.
- Playwright after: runtime dependency, exact `1.60.0`; lockfile root classification matches.
- No browser package, postinstall hook, automatic download, or browser binary was added.
- Canonical example root: `examples/tutorial-browser/`.
- Canonical files: `index.html`, `prepare.mjs`, `server.mjs`, `scenario.json`.
- The former files under `tests/fixtures/tutorial-browser/` were promoted rather than maintained as an independent static copy; `tutorialBrowserFixture.ts` now references the canonical example and remains test-only contract/port infrastructure.
- Package `files` now includes the canonical example. Tests, generated media, browser caches, and Chromium binaries remain excluded.
- Required packed owners include `dist/src/browser/playwrightRuntime.js`, `dist/src/runtime/managedProcess.js`, `dist/src/tutorial/runTutorial.js`, `dist/src/tutorial/tutorialSession.js`, `dist/src/tutorial/tutorialArtifacts.js`, `dist/src/tutorial/tutorialManifest.js`, `dist/src/commands/runTutorialValidateCommand.js`, and `dist/src/commands/runTutorialRunCommand.js`.

## Validation evidence

- Installed tutorial help: PASS for `tutorial --help`, `tutorial validate --help`, and `tutorial run --help`.
- Installed tutorial validation: PASS with `status=valid`, scenario `lab-browser-fixture`, and matching target contract.
- Clean-consumer Playwright resolution: PASS at exact `1.60.0`.
- Browser-unavailable run: PASS with `status=browser-unavailable`, cleanup completed, and isolated browser cache unchanged/empty.
- Real packed tutorial: PASS with `status=passed`, all seven steps passed, empty warnings, and empty cleanup errors.
- Packed artifacts: `artifacts/tutorial.webm` 166598 bytes; `artifacts/tutorial.srt` 525 bytes; `artifacts/tutorial.vtt` 519 bytes; `artifacts/tutorial.md` 860 bytes; `artifacts/tutorial-manifest.json` 9421 bytes.
- Packed screenshots: `screenshots/app-open.png` 23272 bytes; `screenshots/activated.png` 27107 bytes; `screenshots/submitted.png` 24321 bytes; `screenshots/dropped.png` 23354 bytes; `screenshots/banner-visible.png` 27810 bytes.
- WebM EBML magic bytes validated. Manifest schema is `1.0.0`, run status is `passed`, canonical artifact statuses are `written`, cleanup errors are empty, and paths are relative POSIX run-root paths.
- Explicit `--out` resolved exactly to the requested run root. Default workspace was beneath `<fake-home>/.my-dev-kit-lab/tutorials/lab-browser-fixture/<run-id>/`; explicit workspace was beneath `<workspace>/tutorials/lab-browser-fixture/<run-id>/`.
- Installed package and packaged example snapshots were byte-for-byte unchanged after all tutorial acceptance.
- `npm pack --dry-run --json`: PASS; canonical example and representative compiled owners present, no `tests/`, generated media, or browser binaries shipped.

## Test and gate results

- `npm run typecheck`: PASS.
- Focused package tests: PASS, 22 tests in 2 files.
- Focused tutorial tests: PASS, 319 tests in 20 files, including real Chromium integration.
- `npm run test`: PASS.
- `npm run verify`: PASS.
- `npm run verify:packed-package`: PASS from exact npm tarball and clean consumer.
- `npm run docs:check`: PASS.
- `npm audit`: PASS with two existing moderate dev-only Vitest dependency-chain findings.
- `npm audit --omit=dev`: PASS, 0 vulnerabilities; no runtime blocker.
- Chromium was already available for exact Playwright `1.60.0`; no install command was needed.

## Scope and preservation

- Canonical documentation files were not modified. CI and pre-release workflows were not modified.
- No new tutorial actions, assertions, locator kinds, schema fields, FFmpeg/MP4/audio/video muxing, gallery integration, Observer production code, browser-install CLI, version bump, release branch, tag, publication, push, or GitHub Release was created.
- Preserved staged reports: `docs/reports/my-dev-kit-lab-retrieval-for-tutorial-video-automation.md` and `docs/reports/v0.4.7-documentation-reconciliation-completeness-audit.md`; neither belongs in the correction commit.

## Remaining risk and next stage

- The dev-inclusive audit still reports two moderate vulnerabilities in the existing Vitest dependency chain; runtime audit is clean and no unrelated upgrade was performed.
- Final verdict: `PASS` for the focused installed-package correction.
- Next allowed stage: repeat the v0.4.7 documentation reconciliation and implementation-completeness audit.