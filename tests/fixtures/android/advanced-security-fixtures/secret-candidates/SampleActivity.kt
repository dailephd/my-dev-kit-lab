package com.example.fixtures

// Fixture only — every value below is fake and unmistakably not a real
// credential. Used by later v0.4.1 secret-candidate scanner batches.
class SampleActivity {
    private val apiToken = "FAKE_TOKEN_1234567890ABCDEFGHIJ"
    private val password = "changeme-fake-password"
    private val placeholderSecret = "your-api-key-here"
}
