package com.example.fixtures;

// Fixture only — every value below is fake and unmistakably not a real
// credential. Used by later v0.4.1 secret-candidate scanner batches.
public class SampleActivity {
    private static final String SECRET_TOKEN = "FAKE-DUMMY-TOKEN-0000000000000000";
    private static final String DB_PASSWORD = "test-fake-db-password";
}
